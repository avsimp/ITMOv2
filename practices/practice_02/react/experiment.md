# ReAct

> Исправление по ревью: отдельный `TRAINING_PR_INJECTION.diff` удалён из сдачи. Для повторения используйте [актуальный протокол и подготовку временного входа E5.2](../../practice_01/tests_e2e.md#подготовка-входа-e52). Ниже сохранена историческая запись эксперимента: прежние хеши, номера строк, команды и результаты относятся к версии до исправления, а не к новому входу.

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Цель: проверить, что все упоминания E5 в файлах Практики 1 согласованы с обновлённым разделом E5 в `tests_e2e.md` (после Few-shot/R.C.T.F./CoV/ToT/RAG) — нет противоречащих чисел, неактуальных ссылок или устаревших формулировок.
- Доступные входы: файлы `practices/practice_01/*.md` (текстовый поиск и чтение), команда `grep -rn` для поиска упоминаний "E5".
- Разрешённые действия: `grep` по `.md`-файлам Практики 1; чтение найденных строк с контекстом; сравнение формулировки с текущим текстом `tests_e2e.md`.
- Запрещённые действия: изменение кода приложения (его нет); правка файлов без предъявления конкретного найденного расхождения; использование источников вне `practices/practice_01/*.md` и `practices/practice_02/*/experiment.md`.
- Максимальное число шагов: 6 (Thought/Action/Observation).
- Условие остановки и вопроса человеку: остановиться после 6 шагов или раньше, если использованы все документы, где упоминается "E5" (полный список получен на шаге 1); при расхождении — не исправлять самостоятельно формулировки других файлов (`adr.md`, `analysis.md`, `problem.md`, `product_management.md`, `project_management.md`), а сообщить о находке автору и спросить, что делать, до внесения правки.

## Запрос

```text
Проверь согласованность всех упоминаний "E5" в файлах practices/practice_01/*.md
с текущим (обновлённым за 5 экспериментов) разделом E5 в tests_e2e.md. Работай по
циклу Thought/Action/Observation, не более 6 шагов. На каждом шаге указывай
действие, наблюдение и решение продолжать/остановиться. Не исправляй другие файлы
самостоятельно — при найденном расхождении сформулируй вопрос автору.
```

## Наблюдаемые действия и результаты

| Шаг | Действие | Наблюдение | Решение продолжить или остановиться |
|---|---|---|---|
| 1 | `grep -rln "E5" practices/practice_01/*.md` | Найдено 6 файлов: `adr.md`, `analysis.md`, `problem.md`, `product_management.md`, `project_management.md`, `tests_e2e.md`. Список конечен — можно проверить каждый | Продолжить: осталось 5 файлов кроме самого `tests_e2e.md` |
| 2 | Прочитать строку с E5 в `adr.md` (строка 35: «E5 включает вредоносную инструкцию внутри синтетического diff и ручную проверку доказательств») | Формулировка согласуется с E5.2 в `tests_e2e.md` (вход `TRAINING_PR_INJECTION.diff`, критерий «инъекция не исполнена»). Расхождений нет | Продолжить: следующий файл `analysis.md` |
| 3 | Прочитать строку с E5 в `analysis.md` (строка 43: «U3 проверяет включение инструкций; E5 проверяет качество реального ответа; M3») | Формулировка общая и не противоречит новому делению на E5.1/E5.2 — обе части относятся к «качеству реального ответа». Расхождений нет | Продолжить: следующий файл `problem.md` |
| 4 | Прочитать строку с E5 в `problem.md` (строка 19, M3: «Ручная проверка файла/условия/доказательства и максимум 3 рисков; E5») | **Найдено потенциальное расхождение**: формулировка «максимум 3 рисков» здесь совпадает с текущим R7 (0–3 риска), но не упоминает обязательный риск `KeyError`, добавленный как отдельное жёсткое требование в E5.1/E5.2 при Few-shot. Это не противоречие (M3 просто менее детальна), но стоит уточнить у автора, нужно ли усиливать M3 | Продолжить: осталось проверить `product_management.md` и `project_management.md`, затем задать вопрос |
| 5 | Прочитать строки с E5 в `product_management.md` (строка 17, US2: «AC: R3–R5, R7; ... E1, E4–E5») и `project_management.md` (строка 10 и строка 33: «DoD всей реализации включает E5 с реальной моделью на синтетических данных; stub не подтверждает качество генерации») | Обе формулировки согласуются с обновлённым E5: `product_management.md` ссылается на R7 (актуально), `project_management.md` подтверждает «реальная модель» и «синтетические данные», что соответствует E5.1 (`TRAINING_PR.diff`) и E5.2 (`TRAINING_PR_INJECTION.diff`). Расхождений нет | Остановиться: все 5 файлов (кроме `tests_e2e.md`) проверены за 5 шагов из лимита 6 |
| 6 (итог, не отдельное действие) | Сформулировать вопрос автору по находке шага 4 | Единственная находка — `problem.md`/M3 не упоминает обязательный риск `KeyError` явно, в отличие от детализированного E5.1/E5.2 | Остановка: лимит по факту не исчерпан (использовано 5 действий из 6), но все источники "E5" исчерпаны — дальнейших действий не требуется |

## Что изменили в исходном артефакте

- Файл и раздел: [`problem.md`](../../practice_01/problem.md), таблица «Метрики», строка **M3**.
- Изменение: в колонку «Цель» добавлено явное указание, что на двух учебных примерах (`TRAINING_PR.diff` и `TRAINING_PR_INJECTION.diff`) обязательно также обнаружение риска `KeyError` по критериям [E5.1/E5.2](../../practice_01/tests_e2e.md#под-случаи-e5), а для произвольных diff вне этого набора такое требование не предъявляется. В колонку «Источник и окно» добавлена ссылка на раздел «Под-случаи E5».
- Как проверили: автор подтвердил находку шага 4 (M3 не упоминала обязательный риск KeyError) и согласовал формулировку через диалог; правка не меняет числовые пороги M3 (100% на двух примерах, максимум 3 риска), а только явно связывает их с уже существующим требованием E5.1/E5.2.
- Что отклонили: самостоятельную правку без согласования с автором — по правилам ReAct агент сначала задал вопрос, правка внесена только после ответа автора.
