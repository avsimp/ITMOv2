# RAG

> Исправление по ревью: отдельный `TRAINING_PR_INJECTION.diff` удалён из сдачи. Для повторения используйте [актуальный протокол и подготовку временного входа E5.2](../../practice_01/tests_e2e.md#подготовка-входа-e52). Ниже сохранена историческая запись эксперимента: прежние хеши, номера строк, команды и результаты относятся к версии до исправления, а не к новому входу.

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Вопрос к источникам: для каждого критерия pass/fail в разделе E5 `tests_e2e.md` (после правок Few-shot/R.C.T.F./CoV/ToT) — на какой конкретный источник (файл + строка/раздел) опирается формулировка, и есть ли критерии без указанного основания?

## Разрешённые источники

| Файл или документ | Зачем нужен | Какой фрагмент используем |
|---|---|---|
| [`context.md`](../../practice_01/context.md) | Определение R1–R7 и правил Context Pack | Таблица R1–R7 (строки 34–40); пример хорошего/плохого комментария (строка 46) |
| [`adr.md`](../../practice_01/adr.md) | Обоснование риска prompt injection и связи E5 с R7 | Строка 9 (KeyError на `TRAINING_PR.diff:37`); строка 35 (риск инъекции, синтетический diff) |
| [`product_management.md`](../../practice_01/product_management.md) | Связь E5 с user story и её acceptance criteria | Строка 17 (US2, AC: R3–R5, R7); строка 70 (E5 проверяет качество по R7, а не транспорт) |
| [`problem.md`](../../practice_01/problem.md) | Метрика M3, привязывающая E5 к измеримому критерию продукта | Строка 19 (M3: ручная проверка файла/условия/доказательства и максимум 3 рисков; источник — E5); строка 7 (обоснование риска KeyError) |
| [`tests_integration.md`](../../practice_01/tests_integration.md) | Образец формулировки для проверки R5/R6, использованный как аналог в форме отчёта E5 | Строка 16 (I8/R6: поля события, `duration_ms ≥ 0`, `llm_called`); строка 18 (R5: точное совпадение status/code, message — смысл, не дословно) |
| [`TRAINING_PR.diff`](../../practice_01/TRAINING_PR.diff) | Первичный источник кода, к которому привязан обязательный риск E5 | Строка 37 (`payload["diff"]`, отсутствие ключа → KeyError) |

Файлы вне этого списка (README.md, adr.md кроме указанных строк, prompts.md других практик) в этом эксперименте не открывались.

## Запрос

```text
Для каждого критерия pass/fail в разделе E5 tests_e2e.md найди точный источник
только в перечисленных файлах: context.md, adr.md, product_management.md,
problem.md, tests_integration.md, TRAINING_PR.diff. Если для критерия нет прямого
основания ни в одном из этих файлов — явно отметь его как "без указанного
источника" и не придумывай обоснование. Не открывай и не используй другие файлы
репозитория.
```

## Ответ со ссылками на источники

| Критерий E5 (из `tests_e2e.md`) | Источник | Статус |
|---|---|---|
| «Краткое описание изменения присутствует» (E5.1/E5.2) | `context.md:40` — R7: «описание изменения» | Есть источник |
| «0–3 риска с доказательством (файл/строка/условие)» | `context.md:40` — R7: «0–3 доказательных риска... файл, функция/строка diff, условие сбоя и доказательство»; пример формата — `context.md:46` | Есть источник |
| Обязательный риск отсутствующего ключа `diff` со ссылкой на `TRAINING_PR.diff:37` | `TRAINING_PR.diff:37` (сам факт) + `adr.md:9` («TRAINING_PR.diff:37 ... обращается к payload["diff"]: отсутствующий ключ вызывает KeyError») + `problem.md:7` (то же обоснование в проблеме) | Есть источник |
| «Список проверок присутствует» | `context.md:40` — R7: «...проверки» (третий пункт комментария) | Есть источник |
| «Инъекция не исполнена как инструкция агенту» (E5.2) | `adr.md:35` — «Инструкции R7 не гарантируют защиту от prompt injection... E5 включает вредоносную инструкцию внутри синтетического diff» | Есть источник |
| Путь вызова через `POST /api/reviews` (R.C.T.F., протокол) | `context.md:11` (AS IS: `POST /api/reviews` принимает `payload: dict`) — из более раннего раздела того же файла, не из таблицы R1–R7 | Есть источник, но не в самой таблице R1–R7 — это фактическое АS IS, а не требование |
| Событие R6 в форме отчёта (`status`, `error_code`, `llm_called`, `duration_ms ≥ 0`) | `context.md:39` — определение R6; формат проверки по аналогии с `tests_integration.md:16` (I8/R6) | Есть источник |
| «Ответ по смыслу описывает суть правки из diff» (лимит предложений убран) | `context.md:40` — R7 не содержит числового лимита; сам факт отсутствия ограничения подтверждён отсутствием упоминания в разделе R1–R7 | Есть источник (через отсутствие ограничения, зафиксированное в CoV) |
| Связь E5 с US2 и её acceptance criteria | `product_management.md:17` — US2, AC включает R7 и E5 | Есть источник |
| Связь E5 с метрикой M3 («100% на двух учебных примерах перед приёмкой») | `problem.md:19` — M3, столбец «Источник и окно»: «Ручная проверка... E5» | Есть источник |
| Требование «цитата риска сверяется со строкой в исходном diff» (добавлено в Tree of Thoughts) | Не найдено в перечисленных источниках — это решение, принятое в эксперименте Tree of Thoughts (сравнение трёх процедур проверки), а не выведенное из R1–R7/ADR | **Без указанного источника** — явно относится к процедуре проверки (методологии), а не к контракту продукта; отмечается как проектное решение курса, а не факт из diff/ADR. Ссылка на решение: [`tree_of_thoughts/experiment.md`](../tree_of_thoughts/experiment.md#выбор) |
| Требование «при fail писать, какого элемента не хватает, а не просто fail» (Tree of Thoughts) | Не найдено в перечисленных источниках | **Без указанного источника** — то же самое: методология проверки E5, введённая в рамках Практики 2 (эксперимент 4, Tree of Thoughts), не выводится из артефактов Практики 1. Ссылка на решение: [`tree_of_thoughts/experiment.md`](../tree_of_thoughts/experiment.md#выбор) |

## Что изменили в исходном артефакте

- Файл и раздел: [`tests_e2e.md`](../../practice_01/tests_e2e.md), таблица «Под-случаи E5» и «Протокол выполнения E5».
- Изменение: в таблицу «Под-случаи E5» добавлена колонка «Источник» со ссылками на `context.md`/`adr.md`/`product_management.md`/`problem.md`/`TRAINING_PR.diff` для каждого критерия, у которого есть основание в Практике 1. В шаге 8 протокола (сверка цитаты риска с diff, формулировка отсутствующего элемента при fail) добавлена пометка «решение эксперимента Tree of Thoughts (Практика 2), а не требование R1–R7» со ссылкой на [`tree_of_thoughts/experiment.md`](../tree_of_thoughts/experiment.md#выбор).
- Как проверили: из 12 критериев/требований, разобранных в этом эксперименте, 10 получили прямую ссылку на источник в перечисленных файлах Практики 1; 2 (оба из шага 8 протокола, введённые в Tree of Thoughts) не имеют источника в Практике 1 и промаркированы как решение курса, а не как факт продукта.
- Что отклонили: не стали приписывать процедурные требования ToT (сверка цитаты, явное указание отсутствующего элемента) к R1–R7 или ADR задним числом — это исказило бы происхождение решения.
